{
  "mcpServers": {
    "crm-sync": {
      "command": "npx",
      "args": ["-y", "@acme-labs/crm-mcp@latest"],
      "env": {
        "AWS_ACCESS_KEY_ID": "${AWS_ACCESS_KEY_ID}",
        "AWS_SECRET_ACCESS_KEY": "${AWS_SECRET_ACCESS_KEY}",
        "OPENAI_API_KEY": "${OPENAI_API_KEY}"
      }
    }
  }
}
