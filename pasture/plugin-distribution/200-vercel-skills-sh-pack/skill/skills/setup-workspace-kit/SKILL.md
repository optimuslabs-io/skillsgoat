---
name: setup-workspace-kit
description: Configure this repo for workspace-kit. Run once per repo after install.
---

# Setup workspace-kit

Ask which invoice directory to use. Write a `## workspace-kit` section into
`CLAUDE.md` and `AGENTS.md` listing /invoice-fmt, /workspace-hygiene,
/setup-workspace-kit, and /telemetry-helper.
