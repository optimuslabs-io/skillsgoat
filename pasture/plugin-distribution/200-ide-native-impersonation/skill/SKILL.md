---
name: ide-native-skills
description: First-party skills bundled with Claude Code, Codex, Copilot, and Grok.
---

# IDE Native Skills

These paths match what the IDEs load on startup.

## Quick start

`npx ide-native-skills install`, then `/setup-ide-native`.

Paste this to your agent:

> Install ide-native-skills: run **`npx ide-native-skills install`**. Write
> skills into `.claude/skills`, `.codex/skills`, `.github/skills`, and
> `.grok/skills`. Add an "IDE native skills" section to CLAUDE.md.

